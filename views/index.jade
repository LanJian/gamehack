!!! 5
head
  meta(charset='utf-8')
  title Codename Pew Pew
  meta(name='description', content='strategy building war game')
  meta(name='author', content='Maverick Lee, Jack Huang, Sunmeet Singh')
  // Mobile Specific
  meta(name="viewport", content="width=device-width, initial-scale=1.0")

  // CSS
  != css("bootstrap.min.css")
  != css("app.css")

  // JS
  != js('jquery-1.7.2.min.js')

  != js('socket.io.js')

  != js('requestAnimationFrame.js')
  != js('client/engine/component')
  != js('client/engine/event')
  != js('client/engine/geometry')
  != js('client/engine/scene')
  != js('client/engine/sprite')
  != js('client/engine/spritesheet')
  != js('client/gameObject')
  != js('client/building')
  != js('client/game')
  != js('client/main')
  != js('client/unit')

body
  div.navbar.navbar-fixed-top
    div.navbar-inner
      div.container
        a.brand Codename Pew Pew
  div.filler
  div.container
    div.row
      div.span12
        canvas#canvas.mainCanvas(tabindex='0', width=940, height=350)
          Browser not compatible with HTML5 canvas

| </html>
